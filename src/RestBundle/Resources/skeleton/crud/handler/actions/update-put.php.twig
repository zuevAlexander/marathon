
    /**
     * @inheritdoc
     */
    public function processPut({{ entity.name }}UpdateRequest $request): {{ entity.name }}
    {
        return $this->{{ entity.name_lcfirst }}Service->updatePut($request);
    }
