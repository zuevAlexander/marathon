
    /**
     * @inheritdoc
     */
    public function processPatch({{ entity.name }}UpdateRequest $request): {{ entity.name }}
    {
        return $this->{{ entity.name_lcfirst }}Service->updatePatch($request);
    }
